[project]
name = "policygate"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["fastapi>=0.115,<1", "httpx>=0.27,<1", "pydantic>=2.7,<3", "PyYAML>=6.0,<7"]

[project.scripts]
policygate = "src.cli:main"